# RPG Raylib

Build: .\setup.ps1, cmake --preset ninja, cmake --build --preset ninja
Run: .\build\bin\main.exe (from root dir)

Unity build. New .cpp needs cmake reconfigure.
Deps auto-download to lib/. No tests.
